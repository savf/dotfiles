# This is Git's per-user configuration file.
[user]
# Please adapt and uncomment the following lines:
	name = Stephan Mannhart
	email = stephan.mannhart@starmind.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
	process = git-lfs filter-process
[core]
  editor = code --wait
	hooksPath = /Users/stephanmannhart/.git_template/hooks
[diff]
  tool = opendiff
